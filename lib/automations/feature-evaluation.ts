/**
 * 【ATLAS機能評価】
 *
 * 機能名：ホーム「この画面で定期登録」からのX定期投稿（実投稿まで）
 * ユーザー価値：決まった時刻に人が手を動かさずXへ投稿でき、習慣的な投稿作業が減る
 * 差別化：投稿文生成だけで終わらず、連携検証・サーバー実行・実投稿・履歴・通知まで一本化
 * 繰り返し作業の削減：はい（毎日/毎週の投稿作成・投稿操作・確認の繰り返し）
 * AI必要度：中 — 投稿文の生成・トーン調整のみ。時刻判定・排他・投稿・履歴は通常プログラム
 * AIなしで実装可能：一部 — スケジュール・トークン検証・X API・通知は通常プログラム
 * 運営コスト：実行頻度ぶんのLLM呼び出し（エコモード時はまとめて生成可）。Cronは毎分ポーリング
 * 外部APIコスト：有 — X API（投稿時のみ）・必要時のトークンリフレッシュ
 * コスト削減案：
 *   - エコモード
 *   - まとめて生成（SNSバッチ）
 *   - キャッシュ再利用
 *   - 予約実行（nextRun / Vercel Cron）
 *   - AI起動条件（due かつ有効な定期仕事のみ）
 *   - 外部API最小化（投稿確定時のみ X API）
 *   - 承認後実行（投稿前確認モード）
 *   - 同じ処理を再生成しない（idempotency / tick claim）
 * 優先度：P0
 */

export const X_RECURRING_POST_FEATURE_EVALUATION = {
  name: "x_recurring_post_from_home",
  priority: "P0",
  aiRequired: "medium",
} as const;
