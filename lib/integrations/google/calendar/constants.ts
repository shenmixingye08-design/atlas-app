export const CALENDAR_API_BASE = "https://www.googleapis.com/calendar/v3";

/** Minutes before an event when automation should notify the user. */
export const DEFAULT_CALENDAR_NOTIFY_MINUTES_BEFORE = 15;
