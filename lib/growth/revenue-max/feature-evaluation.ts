/**
 * 【ATLAS機能評価】
 *
 * 機能名：収益最大化（初回価値→有料化→継続→解約実測）
 * ユーザー価値：登録後すぐ成果を出し、必要なときだけ実在プランを案内し、解約理由を残す
 * 差別化：全画面広告ではなく実測トリガー。AIは課金・解約を実行しない
 * 繰り返し作業の削減：はい — 初回仕事選び、上限到達時の案内、解約理由の手集計を減らす
 * AI必要度：低 — 改善下書きは実測ルール。課金判断はプログラム
 * AIなしで実装可能：はい（今回の下書きは決定的ルール）
 * 運営コスト：追加の外部有料APIなし。既存 Checkout / Portal / 利用枠を再利用
 * 外部APIコスト：なし（新規）。既存 Stripe / OpenAI は従来どおりユーザー操作時のみ
 * コスト削減案：
 *   エコモード：AI提案を呼ばない
 *   まとめて生成：しない
 *   キャッシュ：variant 固定、イベント dedupe
 *   予約実行：なし
 *   AI起動条件：今回は起動しない
 *   外部API最小化：Checkout は明示操作時のみ
 *   承認後実行：Owner提案は承認しても Stripe を変えない
 *   再生成禁止：初回成功・Checkout は dedupe
 * 優先度：P0（計測と整合）/ P1（案内と解約）/ P2（実験は記録のみ）
 */
export const REVENUE_MAX_FEATURE_NAME = "収益最大化" as const;
