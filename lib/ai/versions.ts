/** Bump when workflow orchestration semantics change — invalidates workflow cache entries. */
export const ATLAS_WORKFLOW_VERSION = "2";

/** Bump when AI policy / model routing changes — invalidates workflow cache entries. */
export const ATLAS_POLICY_VERSION = "1";
