/**
 * 【ATLAS機能評価】
 *
 * 機能名：収益エージェントβ（運営者専用）— 実売上ループ
 * ユーザー価値：一般ユーザー向けではない。1件の投稿から登録・入金までを実測し、次回企画を実績で改善する
 * 差別化：推定クリック／推定売上を出さない。Stripe入金と Clerk 登録の実データだけを帰属する
 * 繰り返し作業の削減：はい — 企画・UTM・帰属・ファネル集計・次回優先度を手作業でつながなくてよい
 * AI必要度：低〜中 — 文案のみ。UTM・重複排除・帰属・集計・権限はプログラム
 * AIなしで実装可能：集計・帰属・承認キューは可能。文案の多様性だけ AI
 * 運営コスト：既存 OpenAI 一括生成と既存 X 投稿のみ。新規の有料外部APIなし
 * 外部APIコスト：既存 OpenAI（生成時）/ 既存 X（承認後）/ 既存 Stripe webhook（観測のみ）
 * コスト削減案：
 *   エコモード：既存の一括生成を維持
 *   まとめて生成：1日1回3案以上
 *   キャッシュ再利用：再生成は明示操作のみ
 *   予約実行：承認・予約後のみ投稿
 *   AI起動条件：文案生成時のみ。集計・帰属にAIを使わない
 *   外部API最小化：新規APIなし。Stripe/Clerk/Xは既存処理を観測
 *   承認後実行：投稿は approved/scheduled のみ
 *   再生成禁止：同じ contentId の成果は dedupeKey で再集計しない
 * 優先度：P1（運営内部β）
 */
export const REVENUE_AGENT_FEATURE_NAME = "収益エージェントβ" as const;
